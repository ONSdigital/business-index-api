# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /                           controllers.HomeController.index
GET     /v1/search                  controllers.v1.SearchController.searchBusiness(suggest:Boolean ?= false)
GET     /v1/suggest                 controllers.v1.SearchController.searchBusiness(suggest:Boolean ?= true)

# Access this route for deployed version information (in JSON).
GET     /version                    controllers.VersionController.version

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
GET     /webjars/*file              controllers.WebJarAssets.at(file)