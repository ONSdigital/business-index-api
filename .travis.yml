sudo: required
dist: trusty
language: scala
scala:
  - 2.11.8
env:
  - JDK=oraclejdk8
  - JDK=openjdk8
addons:
  # https://github.com/travis-ci/travis-ci/issues/5227#issuecomment-165131913
  hosts:
    - ons-bi
  hostname: ons-bi
before_script:
  - jdk_switcher use $JDK